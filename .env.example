# ==============================================================================
# HUST Interface Configuration
# ==============================================================================

# HUST Microsoft SSO Credentials (Optional if setting tokens directly)
HUST_EMAIL="your_name.id@sis.hust.edu.vn"
HUST_PASSWORD="your_secure_password"

# Direct Token & Cookie Overrides (Optional - overrides cached session if set)
ICTSV_TOKEN=""
EHUST_SESSION_COOKIE=""
CTMS_SESSION_COOKIE=""

# Network & Performance Configuration
HTTP_TIMEOUT_SECONDS=15.0
MAX_RETRIES=3
HEADLESS_BROWSER=true

# MCP Server Settings
MCP_HOST="127.0.0.1"
MCP_PORT=8000
